{
  "name": "mammography-camp-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fastify and MongoDB API for Mobile Mammography Camp requests",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/jwt": "^10.2.2",
    "@fastify/rate-limit": "^10.2.1",
    "fastify": "^5.2.1",
    "mongodb": "^6.13.0"
  }
}
